;; -*- coding: utf-8; -*-


(use-package solarized-theme
  :ensure t)
(use-package silkworm-theme
  :ensure t)
(use-package cyberpunk-theme
  :ensure t)
(use-package leuven-theme
  :ensure t)

;; (defun my-install ( pac-list)
;;   "install packages"
;;   (while pac-list
;;   (setq pac (car pac-list))
;;   (setq pac-list (cdr pac-list))
;;   (print pac)
;;   (unless pac
;;     (use-package pac
;;       :ensure t))))

;; (my-install
;;        '(solarized-theme
;; 	silkworm-theme)
;;        )
